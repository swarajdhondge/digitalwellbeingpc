<UserControl x:Class="digital_wellbeing_app.Views.AppUsage.AppUsageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:digital_wellbeing_app.ViewModels"
             >
    <UserControl.DataContext>
        <vm:AppUsageViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Today's App Usage" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>

            <ItemsControl ItemsSource="{Binding TodaysUsage}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="5">
                            <!-- Icon -->
                            <Image Width="32" Height="32" Margin="0 0 10 0"
               Source="{Binding Icon}" />

                            <StackPanel>
                                <TextBlock Text="{Binding AppName}" FontWeight="Bold" />
                                <TextBlock Text="{Binding DurationFormatted}"
                       FontSize="12" Foreground="Gray"/>
                            </StackPanel>
                        </DockPanel>
                    </DataTemplate>

                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
