<UserControl
    x:Class="digital_wellbeing_app.Views.Dashboard.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <Grid Background="#F0F0F0">
        <ScrollViewer
      HorizontalScrollBarVisibility="Disabled"
      VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock
          Text="🔷 Dashboard"
          FontSize="26"
          FontWeight="Bold"
          Foreground="#333"
          Margin="0,0,0,10"/>

                <WrapPanel ItemWidth="240" ItemHeight="120" HorizontalAlignment="Left">

                    <Border
            Background="White"
            Padding="16"
            Margin="8">
                        <Border.Effect>
                            <DropShadowEffect
                Color="#000"
                BlurRadius="10"
                ShadowDepth="2"
                Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock
                Text="Today's Screen Time"
                FontSize="14"
                Foreground="#555"/>
                            <TextBlock
                x:Name="DashboardScreenTime"
                Text="–"
                FontSize="18"
                FontWeight="Bold"
                Foreground="#4caf50"
                HorizontalAlignment="Center"
                Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border
            Background="White"
            Padding="16"
            Margin="8">
                        <Border.Effect>
                            <DropShadowEffect
                Color="#000"
                BlurRadius="10"
                ShadowDepth="2"
                Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock
                Text="Today's Sound Time"
                FontSize="14"
                Foreground="#555"/>
                            <TextBlock
                x:Name="DashboardSoundTime"
                Text="–"
                FontSize="18"
                FontWeight="Bold"
                Foreground="#e57373"
                HorizontalAlignment="Center"
                Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border
            Background="White"
            Padding="16"
            Margin="8">
                        <Border.Effect>
                            <DropShadowEffect
                Color="#000"
                BlurRadius="10"
                ShadowDepth="2"
                Opacity="0.2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock
                Text="Today's App Usage"
                FontSize="14"
                Foreground="#555"/>
                            <TextBlock
                x:Name="DashboardAppTime"
                Text="–"
                FontSize="18"
                FontWeight="Bold"
                Foreground="#42a5f5"
                HorizontalAlignment="Center"
                Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                </WrapPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
