<UserControl
    x:Class="digital_wellbeing_app.Views.Screen.ScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Loaded="ScreenView_Loaded">

    <Grid Background="#F0F0F0">
        <StackPanel Margin="20">

            <!-- Header -->
            <TextBlock
        Text="📊 Detailed Screen Time"
        FontSize="22"
        FontWeight="Bold"
        Foreground="#222"/>

            <!-- Today’s active‐time text -->
            <TextBlock
        x:Name="TodayTimeText"
        Text="00 hr 00 min 00 sec"
        FontSize="18"
        FontWeight="SemiBold"
        Foreground="#4caf50"
        Margin="0,8,0,12"/>

            <!-- Timeline Canvas (green bar) -->
            <Border
        Height="40"
        Background="#DDD"
        Margin="0,0,0,16">
                <Canvas
          x:Name="TimeCanvas"
          Height="40"
          Background="Transparent"/>
            </Border>

            <!-- Weekly Usage Table  -->
            <DataGrid
        ItemsSource="{Binding WeeklyUsage}"
        AutoGenerateColumns="False"
        HeadersVisibility="Column"
        CanUserResizeColumns="False"
        CanUserSortColumns="False"
        RowHeight="32"
        GridLinesVisibility="None"
        IsReadOnly="True"
        Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn
            Header="Day"
            Binding="{Binding Day}"
            Width="*"/>
                    <DataGridTextColumn
            Header="Usage"
            Binding="{Binding Usage}"
            Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</UserControl>
