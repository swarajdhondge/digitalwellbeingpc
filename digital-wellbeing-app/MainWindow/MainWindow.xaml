<Window
    x:Class="digital_wellbeing_app.MainWindow.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Digital Wellbeing"
    Height="750" Width="1100"
    MinHeight="500" MinWidth="800"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="CanResize"
    StateChanged="Window_StateChanged"
    Closing="Window_Closing">

    <!-- Main Window Container with Shadow -->
    <Border x:Name="WindowBorder"
            Background="{DynamicResource Bg.Primary}"
            CornerRadius="8"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <!-- Title Bar -->
                <RowDefinition Height="48"/>
                <!-- Content -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ============================================ -->
            <!-- CUSTOM TITLE BAR -->
            <!-- ============================================ -->
            <Grid Grid.Row="0"
                  Background="{DynamicResource Bg.Primary}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                  MouseLeftButtonUp="TitleBar_MouseLeftButtonUp">

                <!-- Round top corners -->
                <Border CornerRadius="8,8,0,0" Background="{DynamicResource Bg.Primary}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <!-- App Icon + Title -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Spacer -->
                        <ColumnDefinition Width="*"/>
                        <!-- Notification Area (future) -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Window Controls -->
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- App Icon + Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <Image Source="/Resources/Icons/digital-balance-icon-32.png"
                               Width="20" Height="20"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Digital Wellbeing"
                                   Style="{StaticResource HeadingSmall}"
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Notification Area (reserved for future use) -->
                    <StackPanel x:Name="NotificationArea"
                                Grid.Column="2"
                                Orientation="Horizontal"
                                Margin="0,0,16,0"
                                VerticalAlignment="Center">
                        <!-- Empty for now - break reminders use overlay -->
                    </StackPanel>

                    <!-- Window Controls -->
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Top">
                        <!-- Minimize -->
                        <Button x:Name="MinimizeButton"
                                Style="{StaticResource ButtonIcon}"
                                Click="MinimizeButton_Click"
                                ToolTip="Minimize">
                            <md:PackIcon Kind="WindowMinimize" Width="16" Height="16"/>
                        </Button>
                        <!-- Maximize/Restore -->
                        <Button x:Name="MaximizeButton"
                                Style="{StaticResource ButtonIcon}"
                                Click="MaximizeButton_Click"
                                ToolTip="Maximize">
                            <md:PackIcon x:Name="MaximizeIcon" Kind="WindowMaximize" Width="16" Height="16"/>
                        </Button>
                        <!-- Close -->
                        <Button x:Name="CloseButton"
                                Style="{StaticResource ButtonIcon}"
                                Click="CloseButton_Click"
                                ToolTip="Close">
                            <md:PackIcon Kind="WindowClose" Width="16" Height="16"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- ============================================ -->
            <!-- MAIN CONTENT AREA -->
            <!-- ============================================ -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <!-- Sidebar -->
                    <ColumnDefinition Width="220"/>
                    <!-- Content -->
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ============================================ -->
                <!-- SIDEBAR NAVIGATION -->
                <!-- ============================================ -->
                <Border Grid.Column="0"
                        Style="{StaticResource SidebarContainer}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Main Navigation -->
                        <StackPanel Grid.Row="0" Margin="0,8,0,0">
                            <TextBlock Text="OVERVIEW" Style="{StaticResource NavSectionHeader}"/>

                            <Button x:Name="NavDashboard"
                                    Style="{StaticResource NavItem}"
                                    Tag="Active"
                                    Click="Dashboard_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="ViewDashboard" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavScreen"
                                    Style="{StaticResource NavItem}"
                                    Click="ScreenTime_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="Monitor" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Screen Time" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavSound"
                                    Style="{StaticResource NavItem}"
                                    Click="Sound_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="VolumeHigh" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Sound Output" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavApps"
                                    Style="{StaticResource NavItem}"
                                    Click="AppUsage_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="Apps" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="App Usage" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <TextBlock Text="PRODUCTIVITY" Style="{StaticResource NavSectionHeader}"/>

                            <Button x:Name="NavFocus"
                                    Style="{StaticResource NavItem}"
                                    Click="Focus_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="Target" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Focus Mode" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <TextBlock Text="INSIGHTS" Style="{StaticResource NavSectionHeader}"/>

                            <Button x:Name="NavReports"
                                    Style="{StaticResource NavItem}"
                                    Click="Reports_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="ChartBar" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Weekly Report" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <!-- Bottom Navigation -->
                        <StackPanel Grid.Row="2" Margin="0,0,0,16">
                            <Border Style="{StaticResource NavDivider}"/>

                            <Button x:Name="NavHelp"
                                    Style="{StaticResource NavItem}"
                                    Click="Help_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="HelpCircleOutline" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Help" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavSettings"
                                    Style="{StaticResource NavItem}"
                                    Click="Settings_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="Cog" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Settings" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource NavItem}"
                                    Click="Exit_Click">
                                <StackPanel Orientation="Horizontal">
                                    <md:PackIcon Kind="ExitToApp" Width="20" Height="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Exit" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <!-- Version -->
                            <TextBlock x:Name="SidebarVersionText"
                                       Text="v1.7.0"
                                       Style="{StaticResource LabelMuted}"
                                       Margin="24,16,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ============================================ -->
                <!-- MAIN CONTENT -->
                <!-- ============================================ -->
                <Border Grid.Column="1"
                        Background="{DynamicResource Bg.Secondary}"
                        CornerRadius="0,0,8,0">
                    <ContentControl x:Name="MainContent"
                                    Style="{StaticResource AnimatedContentControl}"
                                    Margin="24"/>
                </Border>
            </Grid>

            <!-- ============================================ -->
            <!-- RESIZE GRIP (inside window border) -->
            <!-- ============================================ -->
            <Grid Grid.Row="1" 
                  HorizontalAlignment="Right" 
                  VerticalAlignment="Bottom"
                  Margin="0,0,4,4"
                  Cursor="SizeNWSE"
                  Width="16" Height="16"
                  Background="Transparent"
                  MouseLeftButtonDown="ResizeGrip_MouseLeftButtonDown">
                <Path Data="M 0,12 L 12,0 M 4,12 L 12,4 M 8,12 L 12,8"
                      Stroke="{DynamicResource Text.Tertiary}"
                      StrokeThickness="1.5"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Margin="2"/>
            </Grid>

            <!-- ============================================ -->
            <!-- WIND DOWN MODE VISUAL CUE -->
            <!-- Subtle border glow around window edges -->
            <!-- ============================================ -->
            <Border x:Name="WindDownBorder"
                    Grid.RowSpan="2"
                    BorderThickness="4"
                    BorderBrush="#F59E0B"
                    CornerRadius="8"
                    Opacity="0.3"
                    Visibility="Collapsed"
                    IsHitTestVisible="False"
                    Panel.ZIndex="500">
                <Border.Effect>
                    <BlurEffect Radius="8"/>
                </Border.Effect>
            </Border>
            
            <!-- ============================================ -->
            <!-- BREAK REMINDER OVERLAY -->
            <!-- ============================================ -->
            <Grid x:Name="BreakOverlay" 
                  Grid.RowSpan="2" 
                  Visibility="Collapsed"
                  Panel.ZIndex="1000">
                
                <!-- Semi-transparent backdrop -->
                <Border x:Name="BreakBackdrop"
                        Background="#80000000"
                        MouseLeftButtonDown="BreakBackdrop_Click"/>
                
                <!-- Centered dialog card (Samsung style: 20px radius) -->
                <Border Background="{DynamicResource Bg.Card}"
                        CornerRadius="{DynamicResource Radius.Card}"
                        Padding="40"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MinWidth="400"
                        MaxWidth="500">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="Black"/>
                    </Border.Effect>
                    
                    <StackPanel>
                        <!-- Icon -->
                        <md:PackIcon Kind="Eye"
                                     Width="48"
                                     Height="48"
                                     Foreground="{DynamicResource Accent.Primary}"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,16"/>
                        
                        <!-- Title -->
                        <TextBlock Text="Time for a break!"
                                   Style="{StaticResource HeadingLarge}"
                                   Foreground="{DynamicResource Text.Primary}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,12"/>
                        
                        <!-- Message -->
                        <TextBlock Text="Follow the 20-20-20 rule: Look at something 20 feet away for 20 seconds."
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{DynamicResource Text.Secondary}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,32"/>
                        
                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <!-- Dismiss Button -->
                            <Button x:Name="OverlayDismissButton"
                                    Content="Dismiss"
                                    Click="OverlayDismissButton_Click"
                                    Style="{StaticResource ButtonSecondary}"
                                    Padding="24,12"
                                    Margin="0,0,12,0"
                                    FontSize="14"/>
                            
                            <!-- Snooze Button -->
                            <Button x:Name="OverlaySnoozeButton"
                                    Content="Snooze 5 min"
                                    Click="OverlaySnoozeButton_Click"
                                    Background="{DynamicResource Accent.Primary}"
                                    Foreground="{DynamicResource Accent.OnAccent}"
                                    BorderThickness="0"
                                    Padding="24,12"
                                    FontSize="14"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                        
                        <!-- Turn Off Link -->
                        <TextBlock x:Name="TurnOffLink"
                                   Text="Turn off reminders"
                                   Foreground="{DynamicResource Text.Secondary}"
                                   TextDecorations="Underline"
                                   HorizontalAlignment="Center"
                                   Margin="0,24,0,0"
                                   FontSize="13"
                                   Cursor="Hand"
                                   MouseLeftButtonDown="TurnOffLink_Click"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ============================================ -->
            <!-- FOCUS MODE WARNING OVERLAY -->
            <!-- ============================================ -->
            <Grid x:Name="FocusWarningOverlay" 
                  Grid.RowSpan="2" 
                  Visibility="Collapsed"
                  Panel.ZIndex="1001">
                
                <!-- Semi-transparent backdrop -->
                <Border x:Name="FocusBackdrop"
                        Background="#80000000"
                        MouseLeftButtonDown="FocusBackdrop_Click"/>
                
                <!-- Centered warning dialog (Samsung style: 20px radius) -->
                <Border Background="{DynamicResource Bg.Card}"
                        CornerRadius="{DynamicResource Radius.Card}"
                        Padding="40"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MinWidth="420"
                        MaxWidth="520">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="Black"/>
                    </Border.Effect>
                    
                    <StackPanel>
                        <!-- Icon -->
                        <md:PackIcon Kind="AlertCircle"
                                     Width="48"
                                     Height="48"
                                     Foreground="{DynamicResource Status.Warning}"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,16"/>
                        
                        <!-- Title -->
                        <TextBlock Text="Focus Mode Active"
                                   Style="{StaticResource HeadingLarge}"
                                   Foreground="{DynamicResource Text.Primary}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,12"/>
                        
                        <!-- App Name -->
                        <TextBlock x:Name="DistractingAppName"
                                   Text="Chrome"
                                   Style="{StaticResource HeadingMedium}"
                                   Foreground="{DynamicResource Status.Danger}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,8"/>
                        
                        <!-- Message -->
                        <TextBlock Text="This app is marked as Entertainment. Stay focused on your work!"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{DynamicResource Text.Secondary}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,32"/>
                        
                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <!-- Back to Work Button -->
                            <Button x:Name="BackToWorkButton"
                                    Content="Back to Work"
                                    Click="BackToWorkButton_Click"
                                    Background="{DynamicResource Accent.Primary}"
                                    Foreground="{DynamicResource Accent.OnAccent}"
                                    BorderThickness="0"
                                    Padding="24,12"
                                    FontSize="14"
                                    Cursor="Hand"
                                    Margin="0,0,12,0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                            
                            <!-- Continue Anyway Button -->
                            <Button x:Name="ContinueAnywayButton"
                                    Content="Continue Anyway"
                                    Click="ContinueAnywayButton_Click"
                                    Style="{StaticResource ButtonSecondary}"
                                    Padding="24,12"
                                    FontSize="14"/>
                        </StackPanel>
                        
                        <!-- Time Remaining -->
                        <TextBlock x:Name="FocusTimeRemaining"
                                   Text="24:35 remaining in focus session"
                                   Foreground="{DynamicResource Text.Muted}"
                                   HorizontalAlignment="Center"
                                   Margin="0,24,0,0"
                                   FontSize="12"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ============================================ -->
            <!-- END SESSION CONFIRMATION OVERLAY -->
            <!-- ============================================ -->
            <Grid x:Name="EndSessionOverlay" 
                  Grid.RowSpan="2" 
                  Visibility="Collapsed"
                  Panel.ZIndex="1002">
                
                <!-- Semi-transparent backdrop -->
                <Border Background="#80000000"
                        MouseLeftButtonDown="EndSessionBackdrop_Click"/>
                
                <!-- Centered confirmation dialog (Samsung style: 20px radius) -->
                <Border Background="{DynamicResource Bg.Card}"
                        CornerRadius="{DynamicResource Radius.Card}"
                        Padding="40"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MinWidth="400"
                        MaxWidth="480">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.4" Color="Black"/>
                    </Border.Effect>
                    
                    <StackPanel>
                        <!-- Icon -->
                        <md:PackIcon Kind="TimerOff"
                                     Width="48"
                                     Height="48"
                                     Foreground="{DynamicResource Status.Warning}"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,16"/>
                        
                        <!-- Title -->
                        <TextBlock Text="End Focus Session?"
                                   Style="{StaticResource HeadingLarge}"
                                   Foreground="{DynamicResource Text.Primary}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,12"/>
                        
                        <!-- Message -->
                        <TextBlock x:Name="EndSessionMessage"
                                   Text="You still have 15:30 remaining. Are you sure you want to end early?"
                                   Style="{StaticResource BodyMedium}"
                                   Foreground="{DynamicResource Text.Secondary}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,32"/>
                        
                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <!-- Keep Going Button -->
                            <Button x:Name="KeepGoingButton"
                                    Content="Keep Going"
                                    Click="KeepGoingButton_Click"
                                    Background="{DynamicResource Accent.Primary}"
                                    Foreground="{DynamicResource Accent.OnAccent}"
                                    BorderThickness="0"
                                    Padding="24,12"
                                    FontSize="14"
                                    Cursor="Hand"
                                    Margin="0,0,12,0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="8"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" 
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                            
                            <!-- End Session Button -->
                            <Button x:Name="ConfirmEndSessionButton"
                                    Content="End Session"
                                    Click="ConfirmEndSessionButton_Click"
                                    Style="{StaticResource ButtonSecondary}"
                                    Padding="24,12"
                                    FontSize="14"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
